{% extends 'base.html.twig' %}

{% block body %}
	<h1>Contact Details</h1>
	<p>Name:
		{{ contact.name }}</p>
	<p>Email:
		{{ contact.email }}</p>
	<p>Message:
		{{ contact.message }}</p>
{% endblock %}
